---
"@microcharts/mcp": patch
---

Re-embed the catalog: `onSelect` now documents the third way a selection clears — a pointer press outside the chart —
alongside re-selecting the unit and `Escape`, and the per-chart interaction note says the same.
