---
"@microcharts/mcp": patch
---

Re-embeds the library stylesheet and chart catalog after the active-state theming channel landed, so the served assets
carry the `--mc-active-*` and `--mc-rest-opacity` rules.
