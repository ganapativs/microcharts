node_modules/
dist/
coverage/
.turbo/
*.tsbuildinfo
.DS_Store
*.log
.vitest/
playwright-report/
test-results/
screenshots/

# bench/results.json is COMMITTED — the frozen baseline `sync-bench.mjs --check`
# compares bench-summary.json against (timings aren't reproducible from a fresh
# CI run, unlike deterministic gzip sizes). Regenerate with `pnpm bench` + commit.
demo.html
.vitest-attachments/
__screenshots__/
TODO
apps/docs/public/vcheck.html

# Internal / local-only — not for the public repo.
# AI tooling (skills, launch config), planning docs, and the pre-launch audit
# trail live locally only; they're untracked so a clean, professional tree ships.
.claude/
.agents/
agent/
.cursor/
.windsurf/
plan/
audit/
# private-workspace changelogs (the published package keeps its root CHANGELOG.md)
apps/docs/CHANGELOG.md
fixtures/next/CHANGELOG.md

# Local scratch work (never committed)
/scratchpad/

# figma-export generated artifacts (regenerate with pnpm figma:export)
scripts/figma-export/out/
