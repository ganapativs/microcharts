---
title: All charts
description: Browse the full microcharts catalog — every shipped chart at word-size, grouped by the data shape and decision each one answers.
---

Every chart shares one grammar: `data` alone always renders something correct, and
the same prop names mean the same thing everywhere. Pick by the shape of your data
and the decision you need someone to make in a single glance.

<Cards>
  <Card
    title="Open the gallery"
    href="/gallery"
    description="The whole catalog, visual — every shipped chart at word-size in a searchable card grid (or a dense specimen sheet). The fastest way to find the right one."
  />
</Cards>

The sidebar lists every chart individually. Not sure which you need? Start from the
[gallery](/gallery) — search by name, data shape, or the question you're answering,
and filter by collection (core, decision, expressive, frontier).

## Two entries per chart

Each chart ships a **static** entry (hook-free, server-component safe, zero client
JavaScript) and an **interactive** entry (`/interactive`) that adds hover, keyboard
navigation, and live announcements. Import whichever the context needs.

```tsx
import { Sparkline } from "@microcharts/react/sparkline"; // static, RSC-safe
import { Sparkline } from "@microcharts/react/sparkline/interactive"; // hover + keyboard
```

The catalog grows toward 100 planned types — the [gallery](/gallery) shows what's
shipped and what's still on the way.
