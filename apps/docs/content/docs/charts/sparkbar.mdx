---
title: SparkBar
description: Compact bars for discrete magnitudes in React, or a win–loss streak of binary outcomes — static SVG and an interactive entry.
---

SparkBar renders discrete values as compact bars from a zero baseline. In `winloss`
mode it collapses each value to a single up/down mark — a streak you can read at a
glance.

<LiveDemo
  label="SparkBar"
  meta="static · 2.21 kB"
  code={`import { SparkBar } from "@microcharts/react/sparkbar";

<SparkBar data={[4, 6, 2, 8, 5, 9]} title="Deploys per day" />`}
>
  <SparkBar data={[4, 6, 2, 8, 5, 9]} width={120} height={32} title="Deploys per day" />
</LiveDemo>

## Install

<Usage chart="sparkbar" />

## Try it

<Playground chart="sparkbar" />

## Interactive

<InteractiveDemo slug="sparkbar" />

## When to use it

- **Good for** — discrete magnitudes, win–loss streaks, period-over-period counts.
- **Avoid for** — a continuous trend shape (use [Sparkline](/docs/charts/sparkline)) or
  hundreds of points, where bars become noise.

## Sizing

`width` and `height` are viewBox units that also set the rendered pixel box. Omit them and
drive the width from CSS for a chart that fills its container — the viewBox keeps the
aspect ratio.

<Sizing chart="sparkbar" />

## Variants

<LiveDemo label="bar (default)" code={`<SparkBar data={[4, 6, 2, 8, 5, 9]} />`}>
  <SparkBar data={[4, 6, 2, 8, 5, 9]} width={130} height={32} title="Magnitude" />
</LiveDemo>

<LiveDemo label="winloss" code={`<SparkBar data={[1, -1, 1, 1, -1, 1, -1]} mode="winloss" />`}>
  <SparkBar
    data={[1, -1, 1, 1, -1, 1, -1]}
    width={130}
    height={32}
    mode="winloss"
    title="Win–loss streak"
  />
</LiveDemo>

<LiveDemo label="label" code={`<SparkBar data={[4, 6, 2, 8, 5, 9]} label="last" />`}>
  <SparkBar
    data={[4, 6, 2, 8, 5, 9]}
    width={130}
    height={32}
    label="last"
    title="With endpoint label"
  />
</LiveDemo>

## Four homes

<FourContexts slug="sparkbar" />

## Accessibility

Announced from the data — the example above reads:

> Deploys per day. Trending up 125%. Range 2 to 9. Last value 9.

Sign is doubled by position (above or below the baseline), never by color alone, so the
chart survives forced-colors and color-blind viewing.

## Props

<PropTable slug="sparkbar" />

## Related

<Cards>
  <Card
    title="Sparkline"
    href="/docs/charts/sparkline"
    description="A line when shape matters more than counts."
  />
  <Card
    title="ActivityGrid"
    href="/docs/charts/activity-grid"
    description="Intensity over a calendar or matrix."
  />
</Cards>
