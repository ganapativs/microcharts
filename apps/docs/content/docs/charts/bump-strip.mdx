---
title: BumpStrip
description: "A word-sized rank trajectory for React — where you rank and which way it's moving, #1 on top, gaps for unranked periods."
---

BumpStrip answers "where do we rank, and which way is it moving?" — position on an
inverted ordinal scale, so #1 sits on top and climbing draws upward. Ranks are not
values: Sparkline would lie about the distance between #2 and #3.

<LiveDemo
  label="BumpStrip"
  sizeOf="bump-strip"
  code={`import { BumpStrip } from "@microcharts/react/bump-strip";

<BumpStrip data={weeklyRanks} title="Category rank" />`}
>
  <BumpStrip
    data={[5, 5, 4, 4, 4, 3, 2, 2, 3, 2, 1, 1]}
    title="Category rank"
    width={220} height={26}
  />
</LiveDemo>

## Install

<Usage chart="bump-strip" />

## Try it

<Playground chart="bump-strip" />

## Interactive

<InteractiveDemo slug="bump-strip" />

## When to use it

- **Good for** — leaderboard rows, category-rank trends in KPI cards.
- **Avoid for** — continuous values (Sparkline), or more than ~15 rank levels.

## Sizing

<Sizing chart="bump-strip" />

## Variants

<LiveDemo
  label="unranked gaps"
  code={`// null = unranked that period — the line breaks, honestly
<BumpStrip data={[2, null, null, 3, 1, 1]} />`}
>
  <BumpStrip data={[2, null, null, 3, 1, 1]} title="With unranked weeks" width={220} height={26} />
</LiveDemo>

<LiveDemo label="shared scale for small multiples" code={`<BumpStrip data={ranks} maxRank={10} />`}>
  <BumpStrip
    data={[8, 7, 7, 6, 4, 4, 3, 3, 2, 2, 2, 1]}
    maxRank={10}
    title="Fixed 10-rank band"
    width={220}
    height={26}
  />
</LiveDemo>

## Four homes

<FourContexts slug="bump-strip" />

## Why this default

Change dots mark only the periods where rank actually moved — flat runs stay quiet, so
the eye lands on the transitions. End labels ("#5" → "#1") anchor the ordinal read
without an axis.

## Accessibility

The accessible name is the full trajectory — **"From #5 to #1 over 12 weeks; best #1."**
The interactive entry steps the periods (**"Week 7 of 12: #2."**).

## Props

<PropTable slug="bump-strip" />
