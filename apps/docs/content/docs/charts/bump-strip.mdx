---
title: BumpStrip
description: "Rank over time on an inverted ordinal scale, with #1 on top and gaps for unranked periods."
---

BumpStrip plots one entrant's rank over a run of periods on an inverted ordinal scale, so #1 sits on top and climbing
draws upward. Ranks are not values: a Sparkline would treat the gap between #2 and #3 as a real distance. End labels
("#5" → "#1") anchor the ordinal read without an axis, and change dots mark only the periods where rank moved, so flat
runs stay quiet and the eye lands on the transitions.

<LiveDemo label="BumpStrip" sizeOf="bump-strip">
  <BumpStrip data={[5, 5, 4, 4, 4, 3, 2, 2, 3, 2, 1, 1]} title="Category rank" width={220} height={26} />
</LiveDemo>

## Install

<Usage chart="bump-strip" />

## Try it

<Playground chart="bump-strip" />

## When to use it

Use it for leaderboard rows and category-rank trends in KPI cards. For continuous values use Sparkline, and keep it
under about 15 rank levels.

## Sizing

<Sizing chart="bump-strip" />

## Variants

<LiveDemo
  label="unranked gaps"
  code={`// null = unranked that period — the line breaks
<BumpStrip data={[2, null, null, 3, 1, 1]} />`}
>
  <BumpStrip data={[2, null, null, 3, 1, 1]} title="With unranked weeks" width={220} height={26} />
</LiveDemo>

<LiveDemo label="shared scale for small multiples" code={`<BumpStrip data={ranks} maxRank={10} />`}>
  <BumpStrip
    data={[8, 7, 7, 6, 4, 4, 3, 3, 2, 2, 2, 1]}
    maxRank={10}
    title="Fixed 10-rank band"
    width={220}
    height={26}
  />
</LiveDemo>

## Edge cases

<LiveDemo label="single ranked period" code={`<BumpStrip data={[3]} maxRank={5} />`}>
  <BumpStrip data={[3]} maxRank={5} title="One period" width={140} height={26} />
</LiveDemo>

<LiveDemo
  label="all unranked"
  code={`// every period null — no line, no dots; the summary says so plainly
<BumpStrip data={[null, null, null]} />`}
>
  <BumpStrip data={[null, null, null]} title="No ranked periods" width={140} height={26} />
</LiveDemo>

<LiveDemo
  label="flat run"
  code={`// rank never moves — no change dots, a level line
<BumpStrip data={[3, 3, 3, 3, 3]} />`}
>
  <BumpStrip data={[3, 3, 3, 3, 3]} title="Steady at #3" width={140} height={26} />
</LiveDemo>

A single ranked period draws its two end labels with no visible line, because there is no trajectory yet. An all-`null`
series renders the frame with neither line nor labels, and the accessible name reports that directly. A flat run draws a
level line with zero change dots, because `dots="changes"` marks only the periods where rank moved.

## Four homes

<FourContexts slug="bump-strip" />

## Accessibility

The accessible name is the full trajectory — **"From #8 to #1 over 12 weeks; best #1."** The interactive entry steps the
periods (**"Week 7 of 12: #2."**).

<InteractionNote slug="bump-strip" />

## Props

<PropTable slug="bump-strip" />
