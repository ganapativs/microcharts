---
title: RugStrip
description: RugStrip marks every raw observation as a tick on one line, with stacked duplicates darkening into density.
---

RugStrip puts every value in a sample on one line as its own tick, so the full spread and the places the values crowd
read straight off the marks. Every tick is one real observation: no binning, no jitter, no thinning. Coincident values
darken through three opacity tiers (35%, 60%, 85%), so a lone tick stays visible while a stack reads as density; the
tiers are bucketed because a browser paints one path's stroke as a single operation, so the overlap compositing you
might expect never happens. `markValue` adds a full-height highlighted tick for one raw value against the field, which
is the strongest single read the strip offers; the regular ticks are inset by one unit so that highlight reads taller
without escaping the box.

<LiveDemo
  label="RugStrip"
  sizeOf="rug-strip"
  code={`import { RugStrip } from "@microcharts/react/rug-strip";

<RugStrip data={salaries} markValue={78} title="Pay band" />`}
>
  <span className="text-lg">
    {"Your offer sits "}
    <RugStrip
      data={[42, 48, 51, 53, 55, 58, 61, 63, 66, 71, 55, 52, 49, 58, 62, 75, 83]}
      markValue={78}
      title="Pay band"
      style={{ width: "7em", height: "0.9em" }}
    />
    {" inside the band."}
  </span>
</LiveDemo>

## Install

<Usage chart="rug-strip" />

## Try it

<Playground chart="rug-strip" />

## When to use it

Use it for a "you are here" read in a pay band, raw spread beside a stat, or margin composition under a Sparkline. Past
about 400 observations, reach for HistogramStrip: a rug promises raw marks and never downsamples. For trends over time
use Sparkline.

## Sizing

<Sizing chart="rug-strip" />

## Variants

<LiveDemo label="markValue" code={`<RugStrip data={field} markValue={62} />`}>
  <RugStrip
    data={[42, 48, 55, 58, 61, 66, 71, 75]}
    markValue={62}
    title="Field plus you"
    style={{ width: 140, height: 14 }}
  />
</LiveDemo>

<LiveDemo
  label="stacked duplicates darken"
  code={`// coincident observations accumulate ink — that IS the density read
<RugStrip data={[50, 50, 50, 50, 62, 71]} />`}
>
  <RugStrip data={[50, 50, 50, 50, 62, 71]} title="Duplicates" style={{ width: 140, height: 14 }} />
</LiveDemo>

<LiveDemo label="vertical" code={`<RugStrip data={field} orientation="vertical" />`}>
  <RugStrip data={[42, 48, 55, 61, 71]} orientation="vertical" title="Vertical rug" style={{ width: 14, height: 80 }} />
</LiveDemo>

## Edge cases

<LiveDemo label="empty" code={`<RugStrip data={[]} title="No observations" />`}>
  <RugStrip data={[]} title="No observations" style={{ width: 140, height: 14 }} />
</LiveDemo>

Empty data draws a centered axis line rather than a blank hole, so the strip still reads as a rug with nothing on it
instead of a layout gap.

<LiveDemo label="single observation" code={`<RugStrip data={[52]} title="One observation" />`}>
  <RugStrip data={[52]} title="One observation" style={{ width: 140, height: 14 }} />
</LiveDemo>

A single value (or an all-equal field) has a zero-span domain; the tick renders at the strip's midpoint instead of
dividing by zero.

<LiveDemo label="markValue outside the domain" code={`<RugStrip data={[42, 48, 55, 61, 71]} markValue={120} />`}>
  <RugStrip data={[42, 48, 55, 61, 71]} markValue={120} title="Above the field" style={{ width: 140, height: 14 }} />
</LiveDemo>

A `markValue` outside the observed domain clamps to the nearest edge. It never escapes the box, and it never rescales
the field around itself.

## Four homes

<FourContexts slug="rug-strip" />

## Accessibility

The accessible name is the distribution: **"8 values from 42 to 75, median 59.5."** The interactive entry walks the
observations in sorted order and announces each one with its rank: **"48 — 2nd of 17."**

<InteractionNote slug="rug-strip" />

## Props

<PropTable slug="rug-strip" />
