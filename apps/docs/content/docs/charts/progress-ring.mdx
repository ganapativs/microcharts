---
title: ProgressRing
description: A progress ring for React — fixed 12-o'clock start, butt caps, and a countdown sweep mode; never a gauge.
---

ProgressRing answers "how complete is this?" at icon size, where a linear bar doesn't
fit. The start angle is fixed at 12 o'clock and the caps are butt-cut — the two quiet
ways rings inflate progress, both removed. It is never a gauge: no needle, no red zone.

<LiveDemo
  label="ProgressRing"
  sizeOf="progress-ring"
  code={`import { ProgressRing } from "@microcharts/react/progress-ring";

<ProgressRing value={0.68} label="percent" size={48} title="Backup" />`}
>
  <ProgressRing value={0.68} label="percent" size={48} title="Backup" style={{ width: 72, height: 72 }} />
</LiveDemo>

## Install

<Usage chart="progress-ring" />

## Try it

<Playground chart="progress-ring" />

## Interactive

<InteractiveDemo slug="progress-ring" />

## When to use it

- **Good for** — tab headers, KPI card corners, cooldowns and retry timers (`sweep`).
- **Avoid for** — precise reads (Progress — the % label is the datum there).

## Sizing

<Sizing chart="progress-ring" />

## Variants

<LiveDemo
  label="sweep (countdown)"
  code={`// the REMAINING wedge shrinks — cooldown semantics
<ProgressRing value={0.68} sweep />`}
>
  <ProgressRing value={0.68} sweep size={40} title="Cooldown" style={{ width: 56, height: 56 }} />
</LiveDemo>

<LiveDemo label="weight" code={`<ProgressRing value={0.68} weight={6} />`}>
  <ProgressRing
    value={0.68}
    weight={6}
    size={40}
    title="Thick ring"
    style={{ width: 56, height: 56 }}
  />
</LiveDemo>

<LiveDemo
  label="locale — German percent spacing ('68 %', not '68%')"
  code={`<ProgressRing value={0.68} label="percent" locale="de-DE" />`}
>
  <ProgressRing
    value={0.68}
    label="percent"
    locale="de-DE"
    title="Localized percent"
    size={40}
    style={{ width: 56, height: 56 }}
  />
</LiveDemo>

## Edge cases

<LiveDemo
  label="past 100% — the ring clamps full, the label carries the true percent"
  code={`<ProgressRing value={1.4} label="percent" title="Over budget" />`}
>
  <ProgressRing
    value={1.4}
    label="percent"
    title="Over budget"
    size={40}
    style={{ width: 56, height: 56 }}
  />
</LiveDemo>

<LiveDemo label="no data" code={`<ProgressRing value={NaN} label="percent" title="Unknown" />`}>
  <ProgressRing
    value={Number.NaN}
    label="percent"
    title="Unknown"
    size={40}
    style={{ width: 56, height: 56 }}
  />
</LiveDemo>

## Four homes

<FourContexts slug="progress-ring" />

## Why this default

Butt caps and a fixed start: rounded caps overstate small fractions, and variable start
angles make identical fractions look different. Past 100% the ring clamps while the
center label carries the true percent — same contract as Progress. A full circle is
drawn as two half-arcs (SVG cannot draw a single 360° arc).

## Accessibility

The accessible name reuses the progress wording — **"68% complete."**, or **"32%
remaining."** in sweep mode. The interactive entry announces only at 25/50/75/100%
threshold crossings — a streaming value never spams the screen reader.

## Props

<PropTable slug="progress-ring" />
