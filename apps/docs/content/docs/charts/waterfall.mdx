---
title: Waterfall
description: A word-sized waterfall chart for React — signed deltas composing into a total, with a zero-anchored total bar keeping the bridge honest.
---

Waterfall answers "how did the deltas compose into the total?" — the P&L bridge, in a
cell. Sign is encoded by vertical direction from the running level AND by valence color,
never color alone.

<LiveDemo
  label="Waterfall"
  sizeOf="waterfall"
  code={`import { Waterfall } from "@microcharts/react/waterfall";

<Waterfall data={steps} start={60} title="Net income bridge" />`}
>
  <Waterfall
    data={[
      { label: "Product", value: 42 },
      { label: "Services", value: 18 },
      { label: "Refunds", value: -12 },
      { label: "Opex", value: -26 },
      { label: "FX", value: 5 },
    ]}
    start={60}
    title="Net income bridge"
    width={220} height={30}
  />
</LiveDemo>

## Install

<Usage chart="waterfall" />

## Try it

<Playground chart="waterfall" />

## Interactive

<InteractiveDemo slug="waterfall" />

## When to use it

- **Good for** — P&L bridges in table cells, net-change decomposition in KPI cards.
- **Avoid for** — unordered category comparison (MiniBar), or more than ~8 steps.

## Sizing

<Sizing chart="waterfall" />

## Variants

<LiveDemo
  label="costs: down is good"
  code={`// cost breakdowns invert valence — decreases are wins
<Waterfall data={costSteps} positive="down" />`}
>
  <Waterfall
    data={[
      { label: "Cloud", value: -8 },
      { label: "Headcount", value: 14 },
      { label: "Vendors", value: -5 },
      { label: "Travel", value: -3 },
    ]}
    start={40}
    positive="down"
    title="Cost bridge"
    width={220}
    height={30}
  />
</LiveDemo>

<LiveDemo label="no total bar" code={`<Waterfall data={steps} start={60} total={false} />`}>
  <Waterfall
    data={[
      { label: "Product", value: 42 },
      { label: "Services", value: 18 },
      { label: "Refunds", value: -12 },
      { label: "Opex", value: -26 },
      { label: "FX", value: 5 },
    ]}
    start={60}
    total={false}
    title="Steps only"
    width={220}
    height={30}
  />
</LiveDemo>

## Four homes

<FourContexts slug="waterfall" />

## Why this default

The zero-anchored total bar stays on by default: a waterfall without a grounded total is
unverifiable — floating bars alone can't be checked against reality. Hairline connectors
carry the running level between steps without adding ink.

## Accessibility

The accessible name states the endpoints and the split — **"From 60 to 87 over 5 steps:
+65 gains, −38 losses."** The interactive entry steps through the bridge (**"Refunds:
−12, running 108."**).

## Props

<PropTable slug="waterfall" />
