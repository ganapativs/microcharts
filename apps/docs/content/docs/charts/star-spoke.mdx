---
title: StarSpoke
description: A star glyph for React — spokes radiate from a center, length is value, and there is deliberately no connecting polygon, so profiles compare honestly.
---

StarSpoke answers "what is this entity's profile across a few metrics — and which entity in a set is
the odd one out?". Spokes radiate from the center and their length encodes each value. There is no
connecting polygon, ever: the enclosed area of a radar chart lies about magnitude and axis order,
and the research is clear that contour-free wins for spotting outliers.

<LiveDemo
  label="StarSpoke"
  sizeOf="star-spoke"
>
  <StarSpoke
    data={[
      { label: "Speed", value: 0.9 },
      { label: "Power", value: 0.6 },
      { label: "Range", value: 0.5 },
      { label: "Cost", value: 0.3 },
      { label: "Ease", value: 0.7 },
    ]}
    dots="tips"
    title="Product profile"
    size={110}
  />
</LiveDemo>

## Install

<Usage chart="star-spoke" />

## Try it

<Playground chart="star-spoke" />

## When to use it

- **Good for** — entity profiles in small multiples, skill / capability comparison.
- **Avoid for** — fewer than 3 metrics (PairedBars) or precise values (MiniBar).

## Sizing

<Sizing chart="star-spoke" />

## Variants

<LiveDemo
  label="vs baseline"
  code={`const metrics = [
  { label: "Speed", value: 0.9 },
  { label: "Power", value: 0.6 },
  { label: "Range", value: 0.5 },
  { label: "Cost", value: 0.3 },
  { label: "Ease", value: 0.7 },
];
const baseline = [0.5, 0.5, 0.5, 0.5, 0.5];

<StarSpoke data={metrics} compare={baseline} dots="tips" />`}
>
  <StarSpoke
    data={[
      { label: "Speed", value: 0.9 },
      { label: "Power", value: 0.6 },
      { label: "Range", value: 0.5 },
      { label: "Cost", value: 0.3 },
      { label: "Ease", value: 0.7 },
    ]}
    compare={[0.5, 0.5, 0.5, 0.5, 0.5]}
    dots="tips"
    title="vs baseline"
    size={110}
  />
</LiveDemo>

`locale` doesn't change any in-chart mark — tip labels are metric names, never formatted numbers —
but it does localize the extremes named in the accessible summary:

<LiveDemo
  label="locale — the accessible summary follows it (no visible mark changes)"
  code={`const metrics = [
  { label: "Speed", value: 1900 },
  { label: "Power", value: 600 },
  { label: "Range", value: 500 },
];

<StarSpoke data={metrics} domain={[0, 2000]} locale="de-DE" />`}
>
  <StarSpoke
    data={[
      { label: "Speed", value: 1900 },
      { label: "Power", value: 600 },
      { label: "Range", value: 500 },
    ]}
    domain={[0, 2000]}
    locale="de-DE"
    title="Localized summary"
    size={110}
  />
</LiveDemo>

## Why this default

Contour-free is the point: a connecting polygon is decoration that changes the read, exaggerating
area for high-variance profiles and hiding the individual spoke lengths that actually matter for
comparison. One shared domain governs every spoke in a glyph — mixed-domain metrics are the caller's
normalization to state, so a set of small multiples stays honestly comparable.

Every spoke sits at a fixed clock position (first at 12 o'clock, clockwise), so the axis order never
shifts between instances — that fixed order, plus the always-on guide spokes, is what keeps a profile
readable even where a metric's label doesn't seat. Tip labels use the same fit-or-drop rule as the
rest of the library: each one only renders if it clears its side of the glyph without heavy clamping,
so a long metric name never gets squeezed into an overlap — it drops cleanly and the shape still reads.

## Edge cases

<LiveDemo
  label="long labels at a small size drop out, not overlap"
  code={`<StarSpoke
  data={[
    { label: "Reliability", value: 0.9 },
    { label: "Throughput", value: 0.6 },
    { label: "Cost efficiency", value: 0.5 },
  ]}
  size={48}
/>`}
>
  <StarSpoke
    data={[
      { label: "Reliability", value: 0.9 },
      { label: "Throughput", value: 0.6 },
      { label: "Cost efficiency", value: 0.5 },
    ]}
    title="Small, long labels"
    size={48}
  />
</LiveDemo>

<LiveDemo label="no data" code={`<StarSpoke data={[]} title="Empty" />`}>
  <StarSpoke data={[]} title="Empty" size={64} />
</LiveDemo>

## Four homes

<FourContexts slug="star-spoke" />

## Accessibility

The accessible name names the extremes — **"5 metrics; highest Speed (0.9), lowest Cost (0.3)."**
The interactive entry rotates focus through the spokes with ←/→, announcing each metric and value.

## Props

<PropTable slug="star-spoke" />
