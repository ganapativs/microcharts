---
title: TimeInRange
description: A time-in-band strip for React — how much of a period stayed inside its corridor, in a fixed semantic zone order, with the in-range percent as the headline.
---

TimeInRange answers "how much of the period was the metric inside its acceptable corridor — and
which side did it miss on?". Zones sit in a fixed semantic order — below, in, above — so the strip
is read by position first and color second. The in-range percent is the one number that matters.

<LiveDemo
  label="TimeInRange"
  sizeOf="time-in-range"
  code={`import { TimeInRange } from "@microcharts/react/time-in-range";

<TimeInRange data={{ below: 9, in: 72, above: 19 }} title="Time in range" />`}
>
  <TimeInRange
    data={{ severeBelow: 2, below: 7, in: 72, above: 15, severeAbove: 4 }}
    title="Time in range"
    width={240}
    height={22}
  />
</LiveDemo>

## Install

<Usage chart="time-in-range" />

## Try it

<Playground chart="time-in-range" />

## Interactive

<InteractiveDemo slug="time-in-range" />

## When to use it

- **Good for** — SLO / uptime corridors, glucose-style time-in-range, thermal or budget bands.
- **Avoid for** — ranking parts (SegmentedBar) or a single ratio (Progress).

## Sizing

<Sizing chart="time-in-range" />

## Variants

<LiveDemo label="clinical column" code={`<TimeInRange data={tir} orientation="vertical" label="all" />`}>
  <TimeInRange
    data={{ severeBelow: 2, below: 7, in: 72, above: 15, severeAbove: 4 }}
    orientation="vertical"
    label="all"
    title="Vertical"
    width={28}
    height={130}
  />
</LiveDemo>

<LiveDemo label="five zones" code={`<TimeInRange data={{ severeBelow, below, in, above, severeAbove }} />`}>
  <TimeInRange
    data={{ severeBelow: 3, below: 8, in: 64, above: 18, severeAbove: 7 }}
    title="Severity tiers"
    width={240}
    height={22}
  />
</LiveDemo>

## Four homes

<FourContexts slug="time-in-range" />

## Why this default

Zone order is semantic and immutable — it is never sorted by magnitude, because the position of a
miss (too low vs too high) is half the reading. Severity tiers are drawn with the same hue at
greater ink weight, so the strip never relies on color alone. Percent labels round with
largest-remainder rounding so they always sum to 100, and the summary reads the same integers as
the label.

## Accessibility

The accessible name leads with the headline and then the misses — **"72% in range, 7% below, 15%
above, 2% severe low, 4% severe high."** The interactive entry roves the zones; each announces its
share (**"in range: 72%."**).

## Props

<PropTable slug="time-in-range" />
