name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm format:check
      - run: pnpm build
      # All three render the built dist/, so they follow the build step.
      # craft:  text escapes / overlap / text-on-mark collisions across the catalog.
      # robust: every chart against the degenerate-input matrix — never throw,
      #         never leak a non-finite number into markup.
      # floor:  every chart stays legible down to half its default size — no
      #         overlapping or spilled labels at ordinary in-product sizes.
      - run: pnpm craft
      - run: pnpm robust
      - run: pnpm floor
      - run: pnpm knip
      # EVERY committed generated artifact — size limits, the size-diff baseline,
      # the docs size/bench numbers, the docs registry snapshots, the MCP catalog
      # — is regenerated here and diffed. One step, so a new generator can only be
      # added by adding it to STEPS in gen-all.mjs, and nothing generated can
      # reach main hand-edited or stale. `--skip-build` reuses the dist/ built
      # above.
      #
      # Ahead of `pnpm size` on purpose: it owns `.size-limit.json`, so the
      # budget gate below runs against a regenerated gate file, not a stale one.
      - run: node scripts/gen-all.mjs --check --skip-build
      - run: pnpm size
      - run: pnpm exec publint
      - run: pnpm exec attw --pack .
      # @microcharts/mcp — the sibling MCP server. Runs after the library build
      # above: its catalog-sync guard compares the committed snapshot against the
      # live registry + the built dist/styles.css, and its render tests import the
      # built @microcharts/react. The build must precede the tests — `dist.test.ts`
      # spawns the built `dist/cli.mjs` and hard-fails in CI if it is missing.
      - run: pnpm --filter @microcharts/mcp typecheck
      - run: pnpm --filter @microcharts/mcp build
      - run: pnpm --filter @microcharts/mcp test
      - run: pnpm --filter @microcharts/mcp exec publint
      - run: pnpm --filter @microcharts/mcp exec attw --pack .

  # A change to packages/mcp must ship with a changeset, or npm keeps serving a
  # stale embedded catalog — changesets does not cascade to a public dependent
  # whose `workspace:^` range still covers the new library version. Needs full
  # history to diff against the base branch, so it is its own small job.
  mcp-release-guard:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      # setup-node activates the repo's `packageManager` via corepack, so pnpm
      # has to exist even though this job only runs a builtins-only node script.
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
      - run: node scripts/check-mcp-changeset.mjs "origin/${{ github.base_ref }}"

  # Per-subpath gzip diff against the base branch, replied as one sticky PR
  # comment. It reads two committed `scripts/size-snapshot.json` files — the
  # `quality` job re-measures the head one against a fresh build, so neither
  # side can be understated and this job needs no build at all.
  #
  # Fails when any subpath grows more than 1%. To land a deliberate increase,
  # label the PR `size-increase-approved` (the report is still posted) and
  # re-baseline the budgets in the same PR.
  size-diff:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0
      # setup-node activates the repo's `packageManager` via corepack, so pnpm
      # has to exist even though this job only runs a builtins-only node script.
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
      - name: Diff sizes against ${{ github.base_ref }}
        id: diff
        # The report has to reach the PR even when the gate fails, so the
        # failure is re-raised in the last step instead of here.
        continue-on-error: true
        env:
          SIZE_DIFF_ALLOW:
            ${{ contains(github.event.pull_request.labels.*.name, 'size-increase-approved') && '1' ||
            '0' }}
        run: node scripts/size-diff.mjs "origin/${{ github.base_ref }}" --out size-report.md
      - name: Job summary
        if: always()
        run: cat size-report.md >> "$GITHUB_STEP_SUMMARY"
      # Forked PRs get a read-only token, so the comment API would 403. The job
      # summary above carries the same report for those.
      - name: Reply on the PR
        if: always() && github.event.pull_request.head.repo.full_name == github.repository
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          PR: ${{ github.event.pull_request.number }}
        run: |
          marker='<!-- microcharts-size-report -->'
          id=$(gh api "repos/$GITHUB_REPOSITORY/issues/$PR/comments" --paginate \
                 --jq "map(select(.body | startswith(\"$marker\"))) | .[0].id // empty")
          if [ -n "$id" ]; then
            gh api -X PATCH "repos/$GITHUB_REPOSITORY/issues/comments/$id" -F body=@size-report.md >/dev/null
            echo "updated comment $id"
          else
            gh api -X POST "repos/$GITHUB_REPOSITORY/issues/$PR/comments" -F body=@size-report.md >/dev/null
            echo "posted a new comment"
          fi
      - name: Enforce the size gate
        if: steps.diff.outcome == 'failure'
        run: |
          echo "::error::A subpath grew by more than 1% — see the size report comment on this PR."
          exit 1

  # Static + core-math suite (jsdom). Its output is pure `renderToStaticMarkup`
  # of deterministic geometry — identical on React 18 and 19 — so it runs ONCE,
  # off the React matrix, and needs no browser (no Playwright container / image
  # pull). React-version behaviour is covered by `test-browser` below.
  test-node:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm vitest run --project core --project dom

  # Interactive entries — hooks, StrictMode double-invoke, useId — are the only
  # surface where React 18 vs 19 can diverge, so the matrix lives HERE.
  test-browser:
    runs-on: ubuntu-latest
    # Playwright's own image ships the browsers preinstalled and matched to
    # @playwright/test 1.61.1, so the browser vitest project needs no per-leg
    # `playwright install` download (same trick as visual.yml).
    #
    # This tag and the package version move TOGETHER — a mismatch fails every
    # leg with "Executable doesn't exist at /ms-playwright/…". npm can ship a
    # release before MCR publishes its image (1.62.0 did), so check
    # https://mcr.microsoft.com/v2/playwright/tags/list before bumping either.
    container:
      image: mcr.microsoft.com/playwright:v1.61.1-noble
    strategy:
      fail-fast: false
      matrix:
        # React is the runtime axis CLAUDE.md mandates (18 + 19, StrictMode on).
        # Node is fixed at 22 — the published ESM is node-20-safe ES2022, so the
        # Node version moves only the dev toolchain, not chart behaviour; the
        # container pins it. (Broader Node coverage, if ever wanted, belongs on a
        # main-only or nightly run, not every PR push.)
        react: ["18", "19"]
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Pin React ${{ matrix.react }}
        # -w: the repo became a pnpm workspace when apps/* was added, so pnpm 11
        # requires an explicit workspace-root flag to add the React-matrix pin to
        # the root (lib) package (ERR_PNPM_ADDING_TO_ROOT otherwise).
        run: pnpm add -D -w react@${{ matrix.react }} react-dom@${{ matrix.react }}
      - run: pnpm vitest run --project browser

  rsc:
    # Checkpoint 1 gate: static charts render server-side with zero client JS.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build # dist/ that the fixture imports
      - run: pnpm --filter @microcharts/fixture-next build
      - run: pnpm --filter @microcharts/fixture-next verify

  # The docs workspace was never on CI, so every guard it owns — the registry
  # snapshots, /catalog.json, /llms.txt, the hero specimen literals, the prose
  # claim checks — could drift on main unnoticed. It is 2.5 k assertions in ~3 s.
  # It consumes the built dist/, so the library build comes first.
  docs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: 24.18.0
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm build
      - run: pnpm --filter @microcharts/docs typecheck
      - run: pnpm --filter @microcharts/docs test

  # Block PRs that introduce dependencies with known vulnerabilities. Only the
  # diff is scanned, so it needs the PR event's base/head — hence the guard.
  dependency-review:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - uses: actions/dependency-review-action@a1d282b36b6f3519aa1f3fc636f609c47dddb294 # v5.0.0
        with:
          # GHSA-frvp-7c67-39w9 — path traversal in @hono/node-server's Windows
          # `serve-static`. It reaches us only as a transitive dependency of
          # @modelcontextprotocol/sdk, which pulls Hono for its Streamable-HTTP
          # transport; @microcharts/mcp imports StdioServerTransport only, so no
          # code path here can reach it. Unfixable downstream: the SDK pins
          # `^1.19.9` and the patch landed in Hono 2.0.5 (a major).
          # REMOVE this entry once the SDK widens to Hono 2 — it is not a
          # standing exemption, and nothing else should be added beside it.
          allow-ghsas: GHSA-frvp-7c67-39w9
