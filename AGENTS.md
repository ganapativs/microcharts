# microcharts — AGENTS.md

**Single source of truth: [CLAUDE.md](CLAUDE.md).** All non-negotiables, stack picks, architecture map, catalog rules,
quality bar, roadmap position, and session working rules live there — read it in full before any work, then read the
specific `plan/` doc for the area you're touching (`plan/README.md` is the index).

This file exists only so Codex/other agents discover the same contract. Do not duplicate content here — duplication
drifts. If a rule needs changing, change CLAUDE.md and the relevant `plan/` doc together, with an entry in
`plan/12-research-audit.md` for any new factual claim.
